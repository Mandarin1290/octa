# Example environment variables for octa
OCTA_ENV=dev
# Override OCTA_ROOT to a custom path if needed
# OCTA_ROOT=/path/to/repo

# FirstRateData ingestion
# Never hardcode this in code; set it in your environment.
# FIRSTRATEDATA_USERID=...
