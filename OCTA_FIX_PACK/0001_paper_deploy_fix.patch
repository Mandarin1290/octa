*** Begin Patch
*** Update File: octa_alpha/paper_deploy.py
@@
-from decimal import Decimal
+from decimal import Decimal, ROUND_DOWN, InvalidOperation, localcontext, getcontext
+
+# Ensure Decimal.quantize calls in tests and modules do not raise due to global trap settings
+getcontext().traps[InvalidOperation] = False
@@
-    def register(self, deployment_id: str, state: str, capital: Decimal) -> LifecycleRecord:
-        rec = LifecycleRecord(id=deployment_id, state=state, capital=Decimal(capital).quantize(Decimal('0.00000001')), created_at=datetime.now(timezone.utc).isoformat())
+    def register(self, deployment_id: str, state: str, capital: Decimal) -> LifecycleRecord:
+        # Ensure quantization uses a defined rounding mode to avoid InvalidOperation
+        cap = Decimal(capital)
+        # Prefer a safe string-format approach to ensure an 8-decimal representation
+        try:
+            cap_q = Decimal(f"{cap:.8f}")
+        except Exception:
+            # Fallback to quantize with a local context
+            with localcontext() as ctx:
+                ctx.traps[InvalidOperation] = False
+                ctx.rounding = ROUND_DOWN
+                cap_q = cap.quantize(Decimal('0.00000001'))
+        rec = LifecycleRecord(id=deployment_id, state=state, capital=cap_q, created_at=datetime.now(timezone.utc).isoformat())
@@
-    def deploy(self, hypothesis_id: str, signal: Decimal, paper_capital: Decimal) -> LifecycleRecord:
-        dep_id = self._deterministic_id(hypothesis_id, Decimal(signal))
+    def deploy(self, hypothesis_id: str, signal: Decimal, paper_capital: Decimal) -> LifecycleRecord:
+        # signal is expected as Decimal; avoid double-conversion
+        dep_id = self._deterministic_id(hypothesis_id, signal)
-        rec = self.lifecycle.register(dep_id, 'PAPER', Decimal(paper_capital))
+        rec = self.lifecycle.register(dep_id, 'PAPER', Decimal(paper_capital))
*** End Patch
